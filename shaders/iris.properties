# Iris-specific properties
# Only iris.features.* keys are recognized by Iris. Other keys like
# iris.requiredVersion, iris.opengl.version, etc. are NOT valid and
# are silently ignored (or may produce warnings).

# Optional features this pack can use when available
iris.features.optional=shadow_culling separate_ao
