#version 460 core
// =====================================================================
// Aurora Shaders - composite1.vsh
// Fullscreen triangle (same as composite.vsh).
// =====================================================================
#include "../lib/fullscreen.glsl"

void main() {
    emitFullscreenTriangle();
}
