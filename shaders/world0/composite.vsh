#version 460 core
// =====================================================================
// Aurora Shaders - composite.vsh (world dimension override)
// Fullscreen triangle (path adjusted for world subdirectory).
// =====================================================================
#include "../lib/fullscreen.glsl"

void main() {
    emitFullscreenTriangle();
}
